---

- name: Clone BOSH repo from Altoros
  git: "repo=https://github.com/Altoros/bosh dest={{bosh_path}} accept_hostkey=yes branch=power"
  sudo: no

- name: Install BOSH gems
  git: "bundle install"
  args:
    # creates: "{{bosh_path}}"
    chdir: "{{bosh_path}}"
  sudo: no
